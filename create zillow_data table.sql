CREATE TABLE zillow_data
(
home_type VARCHAR(50),
home_size INTEGER,
bathrooms INTEGER,
latitude INTEGER,
longitude INTEGER,
year_built INTEGER,
address VARCHAR(50),
zipcode INTEGER,
Appraised_Value INTEGER,
Beds INTEGER

);